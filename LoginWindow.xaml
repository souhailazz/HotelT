<Window x:Class="Hotel.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="400" Width="525" Background="#F0F0F0">

    <!-- Styles -->
    <Window.Resources>
        <!-- Button Style -->
        <Style x:Key="MainButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#007BFF"/>
            <!-- Blue button -->
        </Style>

        <!-- TextBox and PasswordBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="black"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Foreground" Value="black"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <!-- Label and Error Message Style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="black"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <!-- UI Layout -->
    <Grid>
        <StackPanel Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center">

            <!-- Title -->
            <TextBlock Text="Se connecter" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <!-- Email Input -->
            <TextBlock Text="Email:"/>
            <TextBox x:Name="EmailTextBox" Width="250" Height="30" Margin="0,5,0,10"/>

            <!-- Password Input -->
            <TextBlock Text="Password:"/>
            <PasswordBox x:Name="PasswordBox" Width="250" Height="30" Margin="0,5,0,10"/>

            <!-- Error Message -->
            <TextBlock x:Name="ErrorTextBlock" Foreground="Red" Margin="0,10,0,0"/>

            <!-- Login Button -->
            <Button x:Name="LoginButton" Content="Login" Style="{StaticResource MainButtonStyle}" 
                    HorizontalAlignment="Center" Click="LoginButton_Click"/>

        </StackPanel>
    </Grid>
</Window>
